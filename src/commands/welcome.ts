const command = {
    name: "welcome",
    description: "Welcome to the index-san",
    default: true,
    execute() {
        console.log("Welcome to the index-san!");
    },
};

export default command;
